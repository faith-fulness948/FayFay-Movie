export const requiredFieldError = "A required field cannot be empty!" // required field error

export const alreadyExistError = (resource) => `${resource} already exist!`; // already existing resource

export const internalServerError = "Internal Server Error. Try again later!"; // internal server error

export const notFoundError = (resource) => `Cannot find ${resource}`; // not found error

export const successUpdateMsg = (resource) => `${resource} updated successfully!`; // updated successfully

export const successDeleteMsg = (resource) => `${resource} deleted successfully!`; // deleted successfully

export const createSuccessMsg = (resource) => `${resource} created successfully!`; // created successfully

